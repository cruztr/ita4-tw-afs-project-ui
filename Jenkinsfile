pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
               script {
                    bat './npm run build' //run npm build task
               }
            }
        }
    }
}